# 2026-04-09

- 完成计划任务 audit-lifecycle-points：通过代码探索确认页面初始化与清理调用链，覆盖 initializeApp/handlePageLoad 触发源（首次加载、pjax:complete、tab 缓存切换、popstate）、cleanup 顺序（progressBar/gallery/scrollToTop/gameRollCleanup）、页面模块分发链（/、article、game、gallery、password、message）以及多语言快慢路径分支（applyTranslationsIn/applyTranslations）。
- 完成计划任务 create-page-registry：新增 `js/pageRegistry.js`，建立 URL -> 页面初始化模块注册映射，并提供 `runPageModuleByUrl(url, context)` 统一调度入口（含 pjax 与 gameRollCleanup 依赖注入约定），为后续替换 main.js 的 switch 分发做准备。
- 完成后续重构任务：`main.js` 已移除 `switch(currentUrl)`，改为 `runPageModuleByUrl(currentUrl, { pjax, setGameRollCleanup })` 注册表调度；保持 Tab 快速路径 / PJAX 完整路径、popstate 缓存恢复与通用初始化逻辑不变；`langManager.init()` 等待条件由 `window.load` 提前为 DOM ready（`DOMContentLoaded`/非 loading 即刻），并通过 `npm run build` 验证通过。
