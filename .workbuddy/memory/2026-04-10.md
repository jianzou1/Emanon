# 2026-04-10

- 完成 `js/logoRandomizer.js` 渲染管线重构：拆分为"缩放计算（applyLogoScale）+ 逐行渲染（renderLinesSequentially）"双阶段。
- 新增逐行速度控制参数：`initializeRandomLogo({ lineIntervalMs })`，默认 `30ms`，并对非法值做兜底。
- 增加并发保护：引入 `renderTaskId` 中止旧渲染任务，避免重复初始化导致多条逐行动画叠加。
- 调整逐行显示方向为"从上到下扫描"：在 `applyLogoScale` 中按完整 ASCII 高度预计算 `--logo-translate-y`（像素值），避免渲染过程中因高度增长出现"从中间往上推"的视觉位移；`css/logo.css` 与 hover 态统一改为基于该变量的 transform。
- 根据用户新要求"仅逐行出现、不需要推动效果"，移除 `css/logo.css` 中 `.logo .text-container` 的 transform 过渡，仅保留 text-shadow 过渡，消除位移动画感。
- 为彻底消除"上推"视觉，`renderLinesSequentially` 改为"完整文本一次写入 + 通过 `height/overflow` 按行揭示"方案（不再每帧重写切片文本）；并执行 `npm run pack` 同步产物到根目录 `main.js`/`styles.css`，避免源码改动未编译导致页面看起来无变化。
- 最终方案（用户明确要求"画面不允许动，仅文本逐行出现"）：改为 `visibility:hidden/visible` 切换方案——每行用独立 `<span>`、初始 hidden（占位不可见），按定时器逐行切 visible。布局从始至终零变化，CSS `--logo-translate-y` 变量已回退移除，transform 恢复固定 `-50%`。
