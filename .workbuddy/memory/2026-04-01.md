# 2026-04-01

## 任务：项目全面评审（已完成）
- 维度：代码可维护性 + 网站加载速度（网络响应、性能）
- 综合评分：5.6/10
- 核心发现：
  - P0 Bug：scrollToTop.js 崩溃、dailyPopup.js interval=1 配置错误、HTML 结构元素在 body 外、game.html 重复 ID
  - 部署严重缺失：netlify.toml 无缓存头/安全头/构建命令不一致
  - 内存泄漏：6 处事件监听器/定时器无清理（SPA 累积风险）
  - 重复代码：6 组工具函数散落在不同模块
  - 最大可维护性问题：messageBoard.js 727 行需拆分
  - 性能亮点：dataCache 缓存层、SPA 页签零请求切换、空闲预热
- 产出：完整评审报告含 20 条优化方案 + 三阶段路线图

## 任务：P0/P1 问题修复（已完成）
- 用户决策：不拆分 messageBoard.js、不做 Netlify 缓存策略/webpack contenthash
- 共执行 9 项修复，构建验证通过
- **P0 修复（6项）：**
  1. netlify.toml：构建命令改为 `npm run build`，添加安全头（X-Frame-Options/X-Content-Type-Options/X-XSS-Protection/Referrer-Policy/Permissions-Policy）
  2. 7 个 EJS 模板：function.ejs include 从 `</body>` 外移入 body 内；game.ejs 删除重复 `#tips`
  3. scrollToTop.js 完全重写：null guard + 100ms throttle + initScrollToTop/cleanupScrollToTop 分离
  4. dailyPopup.js：interval 1→86400、closePopup 改为闭包模式 closeHandler、移除底部自执行
  5. previewLoader.js：import escHtml/escAttr，icon 和 title 做 XSS 转义
- **P1 修复（5项）：**
  6. utils.js 新增 6 个公共函数（getSystemValue/getLocalizedField/normalizeGameData/parseKeys/parseConfigString/shuffleArray），gameList.js/gameRoll.js/gallery.js 删除本地定义改为 import
  7. progressBar.js：添加 `import langManager`，6 处 `LangManager` → `langManager`
  8. gameRoll.js：添加 cleanup 机制（返回 cleanup 函数），追踪 typewriterTimer/animationRafId；main.js 保存并在 handlePageLoad 中调用
  9. CSS：style.css 移除 `.crt-effect` 的 `will-change: contents`；game.css `#gameResult` transition: all → height
