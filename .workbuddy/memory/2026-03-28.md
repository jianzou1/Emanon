# 2026-03-28

## CSS 移动端优化
- 完成全部 9 个 CSS 源文件的移动端优化（gallery.css 无需修改）
- 修改文件：style.css, password.css, message.css, about.css, article.css, logo.css, game.css, daily.css, progress.css
- 核心改动：响应式断点补全、触摸目标修复、字号提升、hover 保护、overflow-wrap、固定宽度改弹性方案
- 构建验证通过，styles.css 18.3 KiB

## 游戏列表行高修复
- `game.css`：li 和 summary 的 `line-height` 从 12px 提升到 16px
- **根本原因**：`style.css` 中 `ul.tree-view li` 有 `line-height: 0.2 !important`，覆盖了 game.css 的行高设置
- 修复：`style.css` L124 将 `line-height: 0.2 !important` 改为 `line-height: 1.4`（无 !important）
- 构建验证通过

## 游戏列表展开图标与文本垂直对齐修复
- **问题**：98.css 的 `summary::before`（+/- 图标）使用 `float: left`，行高 16px 时图标贴顶、文字居中，导致视觉不对齐
- **修复**（`game.css`）：
  - `.game-list .tree-view details > summary::before` — `float: none; display: inline-block; vertical-align: middle`
  - `.game-list .tree-view ul > li::before` — `top: 11px`（水平连接线适配 16px 行高 + 3px padding）
- 修改范围限定在 `.game-list` 作用域内，不影响其他页面 tree-view
- 构建验证通过
