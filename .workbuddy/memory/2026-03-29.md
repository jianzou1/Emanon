# 2026-03-29

## 页脚移动端文本换行修复
- **问题**：`.status-bar p` 全局设置 `white-space: pre-wrap`，移动端空间不足时文本换行
- **修复**（`css/style.css` 移动端断点）：`.status-bar p` 改为 `font-size: clamp(10px, 2.8vw, 13px); white-space: nowrap`
- 使用 clamp() 让字号根据视口宽度自适应缩放，不截断、不换行
- 构建验证通过
