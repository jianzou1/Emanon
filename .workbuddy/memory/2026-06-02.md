# 2026-06-02

## 项目优化 · 阶段 1（删除/清理类）

完成 P0 死代码清理，零功能变更，构建通过。

### 删除的文件
- `page/about.html` + `ejs/pages/about.ejs`（已被 aboutPopup 弹窗替代）
- `cfg/excel/article_cfg.xlsx`（文章索引改为 frontmatter 自动生成，xlsx 已是误导）

### 代码删除
- `js/dailyPopup.js`：去掉 `window.closePopup`（无人使用）；`interval` 从 `1` 改回 `86400`（注释说24小时实际是1秒，等于每次进首页就弹）
- `js/progressBar.js`：删除 `initProgressSystem`（零调用）
- `js/langManager.js`：删除 `bindDynamicElement` / `clearParams` / `reload` / `configure` / `enableDebug`（全部零调用）
- `js/messageBoard.js`：删除 `activeReplyToNickname` 模块变量（写入但从未读取）
- `js/tips.js`：删除 `languageChanged` 监听（langManager 从未派发该事件）
- `cfg/cdn.json`：删除 css98 第三项（错把 `.js` 文件当 css 写）

### 模板清理（每页都重复输出 → 全删）
- `ejs/templates/function.ejs` & `post/_src/template.html`：移除 `<div class="crt monitor">`、`<div id="loading-container">`、`<button onclick="scrollToTop()">`（改为 `type="button"`，由 scrollToTop.js 用 button.onclick 绑定）
- `post/heart/index.html` 同步清理一份手写残留

### 注意
- post/* 子目录里仍有过期产物，下次跑 `npm run build` 会自动从 template.html 重新生成覆盖
- 验证：`npm run build` 通过（main.js 58.7KB、styles.css 20.2KB），page/ 下不再产出 about.html

## 项目优化 · 阶段 2（修正/一致性类）

零功能变更，构建通过（main.js 仍为 58.7 KiB，源码可读性改善）。

### 修正
- `ejs/pages/index.ejs`：3 处 `data-tips="..."data-lang-id="..."` 缺空格修正
- `js/main.js`：删除末尾误导性"启动应用"注释，改写为指明真实入口在 index.js
- `js/footerLoader.js` + `js/progressBar.js`：5 处 `langManager.translate('errors.*')` 改为内联英文 console.error（这些 key 不在 lang_cfg 注册，原本 translate 拿到的就是 key 本身）
- `js/progressBar.js`：删除整个 `i18n` wrapper（无外部 import 后可安全清理）
- `js/progressBar.js`：`ensureDOMReady` 加注释说明轮询兜底用途（visibilitychange 后 DOM 可能未稳定）
- `js/crtEffect.js`：去掉 `instanceCount` 伪指标（destroy 实际从未在主流程被调用，计数永不归零）
- `js/gameList.js`：清理 `bindLangSwitchListener` 内的 18 行设计草稿注释，留下"事件委托 + 微任务延迟"的真实理由
- `404.html`：给 inline 98.css fallback 加注释，链回 `cfg/cdn.json` 提醒同步
- `js/messageBoard.js`：`formatLocation` → `displayLocation`（实际是兜底显示，不是格式化）

