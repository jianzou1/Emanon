# 2026-04-02

## 关于页面弹窗化重构
- 将"关于"页面从独立 PJAX 页面改为通用模态弹窗
- 新建 `js/aboutPopup.js`，采用 createElement 动态创建模式
- 扩展 `css/daily.css` 公共弹窗选择器（容器/p/按钮/响应式四处）加入 `#about-popup`
- `css/about.css` 新增弹窗独有宽度 450px + 图标容器样式
- `footerLoader.js` 页脚链接改为 `data-about-popup` 触发器
- `main.js` 全局点击委托优先拦截 `[data-about-popup]`
- webpack 构建通过（22 modules，含新增 aboutPopup.js）

## 通用弹窗组件抽取
- 新建 `js/popup.js`：导出 `showPopup(options)` 统一封装弹窗创建/关闭/键盘交互
  - 统一支持 ESC + Enter + 遮罩点击 + 确认按钮 + 标题栏×按钮 五种关闭方式
  - 参数：id / title / titleLangId / bodyHTML / confirmLangId / overlayClose / onClose / onReady
- CSS 重构：`daily.css` 从 ID 选择器组（#welcome-popup, #password-error-popup, #about-popup）→ 通用 class（.popup-window / .popup-confirm-btn）
  - 移动端响应式也改为 `.popup-window` 选择器
  - `password.css` 删除冗余移动端 #password-error-popup 覆盖
- 三个弹窗改造：
  - `dailyPopup.js`：不再 fetch HTML 模板，直接 JS 构建 bodyHTML 传给 showPopup()
  - `password.js`：showPasswordError 使用 showPopup() + onClose 回调
  - `aboutPopup.js`：使用 showPopup() + onReady 回调
- webpack 构建通过（23 modules，含新增 popup.js）
