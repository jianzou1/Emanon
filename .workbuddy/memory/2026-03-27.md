# 2026-03-27

## CSS 审查全部 15 项建议已修改完成

### P0 修复（3 项）
1. **progress.css** — 移除重复的 `background-color: #f3f3f3` 和 `position: relative`
2. **style.css** — `margin: 0 auto; margin-bottom: 5px;` 合并为 `margin: 0 auto 5px;`
3. **progress.css** — 修正 `.progress-text` 注释（原写"绝对定位"实际是 relative）

### P1 修复（5 项）
4. **CSS 变量层** — 在 style.css `:root` 中新增 3 个字体栈变量 + 11 个颜色令牌 + 8 个 z-index 分层变量，全部子模块已引用
5. **font-family 去重** — 28 处硬编码字体栈全部改为 `var(--font-ui)` / `var(--font-body)` / `var(--font-mono)`
6. **z-index 体系化** — 9 处 z-index 改为变量引用（`--z-base` 到 `--z-toast`）
7. **!important 消除** — 从 5 处降至 1 处（仅保留 `ul.tree-view li` 的 `line-height`，因无法通过选择器特异性解决）
   - `game.css` 的 3 个 !important：改用 `.game-list .window li, .game-list li` 提高选择器特异性
   - `game.css` 的 `transition: none !important`：移除 !important（无冲突选择器）
8. **弹窗样式去重** — daily.css 中用 `#welcome-popup, #password-error-popup` 选择器组合抽取约 20 行公共样式，password.css 只保留 `width: 300px` 差异

### P2 修复（7 项）
9. **about.css** — 缩进统一为 2 空格，移除冗余注释
10. **.password 类归位** — 从 about.css 迁移到 password.css
11. **fadeIn 重命名** — `@keyframes fadeIn` → `@keyframes windowPulse`（实际是脉冲效果而非淡入）
12. **冗余注释清理** — 移除 style.css / progress.css / about.css 中大量"/* 字体大小 */"等同义注释
13. **移动端适配** — article.css 小屏 6 列→3 列，daily.css 弹窗 `max-width: calc(100vw - 32px)`，gallery.css padding/margin 适配
14. **颜色一致性** — `#C0C0C0` / `#c0c0c0` 统一为变量 `var(--color-win-gray)`，`#fff` / `#ffffff` 统一为 `var(--color-white)`
15. **style.css** — 保留为 @import 聚合 + 全局样式双职责（当前规模不需要拆分）

### 构建验证
- `npm run pack` 成功，styles.css 从 15625 → 16400 bytes（+0.8KB，来自 CSS 变量声明 + 移动端适配）
- 5 个 @keyframes 全部保留：gridBlink、winnerSpring、flipIn、msgFadeIn、windowPulse
- 24 个 CSS 自定义属性正确输出到产物中

## CSS 交互/表现问题审查
- 发现 12 个可通过编辑 CSS 修复的交互/表现不合理点
- 🔴 2 个严重问题：游戏成就项因选择器 `li[onclick]` 与 `data-has-achievement` 不匹配导致缺少 cursor:pointer；留言评论按钮 hover 颜色与默认色相同（无反馈）
- 🟡 6 个中等问题：文章图标 hover 区域不完整、触摸设备 hover 粘滞（缺 @media(hover:hover)）、画廊模态框缺 cursor 提示、进度条低百分比白字灰底不可读、.explain-content 缺 display:flex、code 标签用了中文字体栈
- 🟢 4 个建议改进：关于页图标无 hover 反馈、.window overflow:hidden 影响弹窗、.window 高度过渡导致 PJAX 切换闪缩、弹窗负百分比 margin

## CSS 交互问题修复（#1、#2、#8）
1. **game.css** — `li[onclick]` → `li[data-has-achievement]`，修复游戏成就项缺少 cursor:pointer
   - 追加修复：`.game-list li` 改为 `.window .game-list li`（特异性 0-2-1），解决去除 !important 后被全局 `.window li`（0-1-1）的 margin-bottom:20px 覆盖导致间距变大的问题
2. **message.css** — `.msg-reply-btn:hover` 颜色从 `--color-text-secondary`（无变化）改为 `--color-win-blue`
3. **style.css** — `code { font-family }` 从 `var(--font-body)` 改为 `var(--font-mono)`，代码片段使用等宽字体
